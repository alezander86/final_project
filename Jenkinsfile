pipeline {
    agent any
    stages {
        stage("build"){

            steps {
                echo 'build the application v2'
            }
        }    
        stage("test"){

            steps {
                echo 'testing the application v2'
            }
        }

        stage("deploy"){

            steps {
                echo 'deploy the application'
            }
        }
    }
}